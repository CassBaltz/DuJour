<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
   <h2><%= link_to "Home", root_path, class: "nav navbar-brand"%></h2>
      <% case %>
      <% when user_signed_in? %>
      
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu_items">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>

      <div class="collapse navbar-collapse" id="menu_items">
        <ul class="nav navbar-nav pull-right">
          <li class="<%= is_active?('/about') %>"><%= link_to "about", about_path %></li>
          <li class="<%= is_active?('/view_stores') %>"><%= link_to "stores map", view_stores_path %></li>
          <li class="<%= is_active?('/past_meals') %>"><%= link_to "past meals", past_meals_path %></li>  
            <li id="fat-menu" class="dropdown white_link">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name.downcase %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "order history", order_history_path(current_user) %></li>
                <li><%= link_to "edit settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, :method => :delete %>
                </li>
              </ul>
            </li>
          </ul>
        </div>
          
          <% when store_signed_in? %>
          <ul class="nav navbar-nav pull-right">
            <% if @meal_active.present? %>
            <li class="<%= is_active?('/store/pickup_list') %>"><%= link_to "pickup list", store_pickup_list_path %></li>
            <li class="<%= is_active?('/store/current_orders') %>"><%= link_to "current orders", store_current_orders_path %></li>
            <% end %>
            <li class="<%= is_active?('/store/past_orders') %>"><%= link_to "past orders", store_past_orders_path %></li>
            <li class="<%= is_active?('/store/meal_index') %>"><%= link_to "upcoming meals", store_meal_index_path %></li>
            <li class="<%= is_active?('/store/signout') %>"><%= link_to "signout", store_signout_path, :data => {:confirm => 'Are you sure you want to signout?'}, :method => :delete %></li>
          </ul>

          <% else %>
              <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu_items">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
           
            <div class="collapse navbar-collapse" id="menu_items">
              <ul class="nav navbar-nav pull-right">
                <li class="<%= is_active?('/view_stores') %>"><%= link_to "stores map", view_stores_path %></li>
                <li class="<%= is_active?('/past_meals') %>"><%= link_to "past meals", past_meals_path, id: "sign_up_trigger" %></li>

<!--need to make the past meals link direct to the signin/signup form with a flash message that says you need to sign up to see past meals-->

                <li class="<%= is_active?('/about') %>"><%= link_to "about", about_path %></li>
                <li class="<%= is_active?('/signin') %>"><%= link_to "sign in", signin_path %></li>
              </ul>
            </div>
          <% end %>
   
  </div>
</nav>

<div class="row">
<div id="header_row"></div>
</div>